services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./receipts:/app/receipts
      - ./.env:/app/.env
    restart: unless-stopped

volumes:
  receipts:
